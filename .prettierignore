node_modules
dist
pnpm-lock.yaml

# Bench reports are generated by `pnpm bench:latency`, not written by hand. Holding generated output
# to a formatter fails on the next run rather than on this one, and reformatting it by hand only
# moves the failure. `pnpm verify` checks `git ls-files`, so these were invisible while untracked and
# turned the check red at the moment they were committed.
docs/bench/
