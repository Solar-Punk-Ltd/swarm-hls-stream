# Long run: 2026-08-05T08:48:52.062Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.25s GOP

**37 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9986**
- distance the media timeline travelled, per second of wall clock: **1.0011**
- media the timeline crossed that no segment carried: **0.45s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -63ms per minute of drift**, against the -19ms per minute measured below, which is 327% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-19ms per minute**, which across 3.1 minutes predicts -0.06s. Positive means later segments measured **more** latency.
- samples sit **0.24s** from that line, root mean square.
- **the slope is not resolvable**: the 0.06s it predicts does not clear twice the 0.24s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.06s** |
| capture to fetchable, p95 | 1.59s |
| capture to fetchable, worst | 2.07s |
| segment, median | 0.27s |
| smallest buffer that would not have stalled | 1.80s |
| recommended buffer (floor + 2s assumed client poll + one segment) | 4.07s |
| **behind live at that buffer** | **4.86s** |

## Does the buffer demand grow

- first third of the run needed **1.80s**, last third needed **1.15s**, a change of -0.65s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **8.62s**, across 1 poll(s), starting 2.1 minutes in.
- the bench itself went at most 8.62s between two polls and 4.53s typically. **Both are measured rather than configured**, and the pair is what bounds the resolution here: the gaps are bimodal, so neither alone describes it.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **32 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 12 | 1.02s | 2.07s | 2.07s |
| 1 | 11 | 1.06s | 1.59s | 1.59s |
| 2 | 14 | 1.04s | 1.41s | 1.41s |
