# Long run: 2026-08-05T05:09:28.496Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**358 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0035**
- distance the media timeline travelled, per second of wall clock: **1.0055**
- media the timeline crossed that no segment carried: **0.36s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -332ms per minute of drift**, against the 33ms per minute measured below, which is -1003% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+33ms per minute**, which across 3.0 minutes predicts +0.10s. Positive means later segments measured **more** latency.
- samples sit **0.45s** from that line, root mean square.
- **the slope is not resolvable**: the 0.10s it predicts does not clear twice the 0.45s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.17s** |
| capture to fetchable, p95 | 2.49s |
| capture to fetchable, worst | 3.58s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 3.08s |
| recommended buffer (floor + 2s poll + one segment) | 5.58s |
| **behind live at that buffer** | **6.25s** |

## Does the buffer demand grow

- first third of the run needed **1.63s**, last third needed **3.08s**, a change of +1.45s.
- **a player configured from this run's opening third would eventually stall**, by 1.45s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **2.99s**, across 2 poll(s), starting 2.2 minutes in.
- the bench itself went at most 2.53s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.58s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 122 | 1.16s | 1.71s | 2.13s |
| 1 | 121 | 1.18s | 2.93s | 3.30s |
| 2 | 115 | 1.17s | 2.59s | 3.58s |
