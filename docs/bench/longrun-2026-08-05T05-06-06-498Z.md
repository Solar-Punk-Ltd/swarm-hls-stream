# Long run: 2026-08-05T05:06:06.498Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 1s GOP

**178 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9967**
- distance the media timeline travelled, per second of wall clock: **0.9971**
- media the timeline crossed that no segment carried: **0.07s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 176ms per minute of drift**, against the 56ms per minute measured below, which is 312% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+56ms per minute**, which across 3.0 minutes predicts +0.17s. Positive means later segments measured **more** latency.
- samples sit **0.69s** from that line, root mean square.
- **the slope is not resolvable**: the 0.17s it predicts does not clear twice the 0.69s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.40s** |
| capture to fetchable, p95 | 3.80s |
| capture to fetchable, worst | 4.09s |
| segment, median | 1.00s |
| smallest buffer that would not have stalled | 3.09s |
| recommended buffer (floor + 2s poll + one segment) | 6.09s |
| **behind live at that buffer** | **7.49s** |

## Does the buffer demand grow

- first third of the run needed **2.85s**, last third needed **3.09s**, a change of +0.25s.
- **a player configured from this run's opening third would eventually stall**, by 0.25s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **3.27s**, across 2 poll(s), starting 2.8 minutes in.
- the bench itself went at most 2.40s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.09s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 61 | 2.40s | 3.60s | 3.85s |
| 1 | 60 | 2.42s | 3.82s | 3.94s |
| 2 | 57 | 2.40s | 3.81s | 4.09s |
