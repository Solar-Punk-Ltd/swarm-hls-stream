# Long run: 2026-08-03T17:47:22.174Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 1s GOP

**45 segments measured across 5.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9965**
- distance the media timeline travelled, per second of wall clock: **0.9975**
- media the timeline crossed that no segment carried: **0.28s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 151ms per minute of drift**, against the 55ms per minute measured below, which is 276% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+55ms per minute**, which across 5.1 minutes predicts +0.26s. Positive means later segments measured **more** latency.
- samples sit **0.64s** from that line, root mean square.
- **the slope is not resolvable**: the 0.26s it predicts does not clear twice the 0.64s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **3.47s** |
| capture to fetchable, p95 | 4.65s |
| capture to fetchable, worst | 5.27s |
| segment, median | 1.00s |
| smallest buffer that would not have stalled | 4.28s |
| recommended buffer (floor + 2s poll + one segment) | 7.27s |
| **behind live at that buffer** | **9.75s** |

## Does the buffer demand grow

- first third of the run needed **4.28s**, last third needed **3.65s**, a change of -0.63s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **34.31s**, across 7 poll(s), starting 3.6 minutes in.
- the bench itself went at most 6.55s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 7 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 7.27s **would have rebuffered there**, since the stall exceeded the buffer plus the segment it was playing.
- when it moved, the gateway's resolved feed index jumped **35 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 7 | 3.27s | 3.77s | 3.77s |
| 1 | 9 | 3.53s | 5.27s | 5.27s |
| 2 | 10 | 3.43s | 4.77s | 4.77s |
| 3 | 8 | 3.47s | 4.42s | 4.42s |
| 4 | 11 | 3.48s | 4.65s | 4.65s |
