# Long run: 2026-08-05T08:42:03.132Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**107 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9841**
- distance the media timeline travelled, per second of wall clock: **1.0008**
- media the timeline crossed that no segment carried: **3.01s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -49ms per minute of drift**, against the 113ms per minute measured below, which is -44% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+113ms per minute**, which across 3.1 minutes predicts +0.34s. Positive means later segments measured **more** latency.
- samples sit **0.49s** from that line, root mean square.
- **the slope is not resolvable**: the 0.34s it predicts does not clear twice the 0.49s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.73s** |
| capture to fetchable, p95 | 3.10s |
| capture to fetchable, worst | 3.91s |
| segment, median | 0.53s |
| smallest buffer that would not have stalled | 3.41s |
| recommended buffer (floor + 2s assumed client poll + one segment) | 5.94s |
| **behind live at that buffer** | **7.14s** |

## Does the buffer demand grow

- first third of the run needed **2.57s**, last third needed **2.94s**, a change of +0.37s.
- **a player configured from this run's opening third would eventually stall**, by 0.37s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **4.84s**, across 1 poll(s), starting 2.7 minutes in.
- the bench itself went at most 4.84s between two polls and 1.58s typically. **Both are measured rather than configured**, and the pair is what bounds the resolution here: the gaps are bimodal, so neither alone describes it.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **9 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 41 | 1.69s | 2.38s | 3.10s |
| 1 | 33 | 1.74s | 3.88s | 3.91s |
| 2 | 32 | 1.76s | 3.44s | 3.48s |
| 3 | 1 | 2.23s | 2.23s | 2.23s |
