# Latency run — 2026-08-02T16:14:48.385Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP
5 segment(s) measured; the split below is the median one, whole.

## What a viewer experiences

| | |
| --- | --- |
| capture to fetchable | **5.94s** |
| player buffer, configured | 10.00s |
| **behind live** | **15.94s** |

## Where it went

| hop | ms | | what |
| --- | ---: | --- | --- |
| segment | 2640 |  | the first frame waiting for its own segment to close at the encoder |
| upload | -390 | ~ | the uploader noticing the segment, downloading it from the engine, and uploading it to Swarm |
| manifestPublish | 208 |  | the SOC feed write that publishes a manifest naming it |
| feedPropagation | 2027 | ~ | the manifest reaching the viewer gateway, plus the wait until the next poll asked for it |
| fetch | 1455 |  | retrieving the segment payload itself through the gateway |

Rows marked `~` are bounded by instants from two different clocks. The uploader host reads 184ms ahead of the bench, give or take 200ms. That skew moves time between those two rows and cancels in the total, so it cannot move the headline figure.

## Every sample

| segment | ref | total |
| ---: | --- | ---: |
| 0 | `f3e7ae41c05d` | 6.56s |
| 2 | `8265896b0245` | 4.37s |
| 4 | `a7e75023412e` | 7.60s |
| 6 | `2ea0d25ce04f` | 4.21s |
| 24 | `5126faa05e30` | 5.94s |

## Self-checks

- publisher pace drift: +589ms per minute of media. This is a direct bias on every total above, in the direction of reporting less latency than there was when it is positive.
- **hops that cannot be true**, meaning an input is wrong rather than a pipeline that is fast:
  - segment 0: upload came out at -747ms
  - segment 2: upload came out at -536ms
  - segment 4: upload came out at -385ms
  - segment 6: upload came out at -592ms
  - segment 24: upload came out at -390ms
  The totals are unaffected; the skew estimate or the log pairing is what to distrust.
