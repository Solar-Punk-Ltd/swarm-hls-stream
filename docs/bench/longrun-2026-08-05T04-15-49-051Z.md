# Long run: 2026-08-05T04:15:49.051Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 1s GOP

**179 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0066**
- distance the media timeline travelled, per second of wall clock: **1.0087**
- media the timeline crossed that no segment carried: **0.38s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -522ms per minute of drift**, against the 22ms per minute measured below, which is -2391% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+22ms per minute**, which across 3.1 minutes predicts +0.06s. Positive means later segments measured **more** latency.
- samples sit **0.72s** from that line, root mean square.
- **the slope is not resolvable**: the 0.06s it predicts does not clear twice the 0.72s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.34s** |
| capture to fetchable, p95 | 3.77s |
| capture to fetchable, worst | 3.98s |
| segment, median | 1.00s |
| smallest buffer that would not have stalled | 2.98s |
| recommended buffer (floor + 2s poll + one segment) | 5.97s |
| **behind live at that buffer** | **7.31s** |

## Does the buffer demand grow

- first third of the run needed **2.98s**, last third needed **2.83s**, a change of -0.15s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.22s**, across 2 poll(s), starting 0.2 minutes in.
- the bench itself went at most 2.22s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.97s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 62 | 2.38s | 3.77s | 3.98s |
| 1 | 60 | 2.31s | 3.73s | 3.92s |
| 2 | 57 | 2.26s | 3.81s | 3.83s |
