# Long run: 2026-08-03T18:08:51.372Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 1s GOP

**46 segments measured across 6.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9957**
- distance the media timeline travelled, per second of wall clock: **0.9958**
- media the timeline crossed that no segment carried: **0.03s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 251ms per minute of drift**, against the 254ms per minute measured below, which is 99% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+254ms per minute**, which across 6.1 minutes predicts +1.52s. Positive means later segments measured **more** latency.
- samples sit **1.29s** from that line, root mean square.
- **the slope is not resolvable**: the 1.52s it predicts does not clear twice the 1.29s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.54s** |
| capture to fetchable, p95 | 6.52s |
| capture to fetchable, worst | 11.05s |
| segment, median | 1.00s |
| smallest buffer that would not have stalled | 10.05s |
| recommended buffer (floor + 2s poll + one segment) | 13.05s |
| **behind live at that buffer** | **16.58s** |

## Does the buffer demand grow

- first third of the run needed **4.21s**, last third needed **10.05s**, a change of +5.84s.
- **a player configured from this run's opening third would eventually stall**, by 5.84s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **34.44s**, across 10 poll(s), starting 4.7 minutes in.
- the bench itself went at most 7.18s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 10 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 13.05s **would have rebuffered there**, since the stall exceeded the buffer plus the segment it was playing.
- when it moved, the gateway's resolved feed index jumped **35 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 9 | 4.03s | 5.21s | 5.21s |
| 1 | 7 | 3.67s | 4.96s | 4.96s |
| 2 | 8 | 5.02s | 6.62s | 6.62s |
| 3 | 6 | 4.49s | 6.28s | 6.28s |
| 4 | 7 | 4.66s | 11.05s | 11.05s |
| 5 | 9 | 4.56s | 6.52s | 6.52s |
