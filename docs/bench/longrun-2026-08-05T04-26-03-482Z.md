# Long run: 2026-08-05T04:26:03.482Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 2s GOP

**90 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0034**
- distance the media timeline travelled, per second of wall clock: **1.0121**
- media the timeline crossed that no segment carried: **1.53s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -724ms per minute of drift**, against the 43ms per minute measured below, which is -1671% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+43ms per minute**, which across 3.1 minutes predicts +0.13s. Positive means later segments measured **more** latency.
- samples sit **0.75s** from that line, root mean square.
- **the slope is not resolvable**: the 0.13s it predicts does not clear twice the 0.75s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.30s** |
| capture to fetchable, p95 | 5.53s |
| capture to fetchable, worst | 6.30s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 4.27s |
| recommended buffer (floor + 2s poll + one segment) | 8.27s |
| **behind live at that buffer** | **10.57s** |

## Does the buffer demand grow

- first third of the run needed **4.27s**, last third needed **3.50s**, a change of -0.77s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.89s**, across 2 poll(s), starting 0.9 minutes in.
- the bench itself went at most 2.60s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 8.27s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 32 | 4.46s | 5.77s | 6.30s |
| 1 | 29 | 4.23s | 5.53s | 5.59s |
| 2 | 29 | 4.21s | 5.50s | 5.53s |
