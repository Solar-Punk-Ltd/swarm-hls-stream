# Long run: 2026-08-05T03:10:56.691Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 1s GOP

**302 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0500**
- distance the media timeline travelled, per second of wall clock: **1.0093**
- media the timeline crossed that no segment carried: **-7.23s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -556ms per minute of drift**, against the -18ms per minute measured below, which is 3103% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-18ms per minute**, which across 3.0 minutes predicts -0.05s. Positive means later segments measured **more** latency.
- samples sit **0.65s** from that line, root mean square.
- **the slope is not resolvable**: the 0.05s it predicts does not clear twice the 0.65s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.22s** |
| capture to fetchable, p95 | 3.50s |
| capture to fetchable, worst | 4.38s |
| segment, median | 0.53s |
| smallest buffer that would not have stalled | 4.12s |
| recommended buffer (floor + 2s poll + one segment) | 6.65s |
| **behind live at that buffer** | **8.34s** |

## Does the buffer demand grow

- first third of the run needed **3.29s**, last third needed **3.28s**, a change of -0.01s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.21s**, across 2 poll(s), starting 1.9 minutes in.
- the bench itself went at most 2.40s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.65s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 112 | 2.35s | 3.49s | 3.81s |
| 1 | 98 | 2.12s | 3.75s | 4.38s |
| 2 | 92 | 2.24s | 3.40s | 3.91s |

## 49 segment(s) reached the bench and could not be read

- `8128edff629d`: cannot measure how much media 8128edff629d753128f093fb90894b06943eb52ad4369ce8bff2a049f1d56793 holds: it holds one video packet, which fixes when a frame started and nothing about how long it lasted. A span assumed here would be indistinguishable in the report from a measured one
- `d5971f0779dc`: cannot measure how much media d5971f0779dc07566dce1284deada6ceac29fb728c3b0f2be40f7cff6ed2477f holds: it holds one video packet, which fixes when a frame started and nothing about how long it lasted. A span assumed here would be indistinguishable in the report from a measured one
- `a7730f47b840`: cannot measure how much media a7730f47b840ecd6dec92ea3de32d4244ec9f8dac53089d8953097f1288e3361 holds: it holds one video packet, which fixes when a frame started and nothing about how long it lasted. A span assumed here would be indistinguishable in the report from a measured one
- `d593a7b5f619`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 32.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `af889e9b7d69`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 34.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `31dac42ff8f4`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 42.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `fa9b8b60b579`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 42.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `5efa3f5291c7`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 47.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `0f7ea77e2664`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 48.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `45da8502dc6c`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 51.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `222018ae3d9c`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 65.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `b7a4c8eb8ed3`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 72.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `44effc966690`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 76.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `9140e6efc90a`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 82.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `a779796a09c2`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 82.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `7e8eae88b962`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 83.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `3f5d9c237eb9`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 88.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `a506afc171c0`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 89.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `7f6566acaf3c`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 91.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `264eb7806a65`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 97.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- ...and 29 more, in the JSON beside this file.
