# Long run: 2026-08-05T02:43:13.942Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**83 segments measured across 1.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0220**
- distance the media timeline travelled, per second of wall clock: **1.0154**
- media the timeline crossed that no segment carried: **-0.31s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -926ms per minute of drift**, against the -503ms per minute measured below, which is 184% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-503ms per minute**, which across 1.0 minutes predicts -0.39s. Positive means later segments measured **more** latency.
- samples sit **0.43s** from that line, root mean square.
- **the slope is not resolvable**: the 0.39s it predicts does not clear twice the 0.43s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.27s** |
| capture to fetchable, p95 | 2.97s |
| capture to fetchable, worst | 3.30s |
| segment, median | 0.43s |
| smallest buffer that would not have stalled | 2.83s |
| recommended buffer (floor + 2s poll + one segment) | 5.27s |
| **behind live at that buffer** | **7.11s** |

## Does the buffer demand grow

- first third of the run needed **2.83s**, last third needed **2.76s**, a change of -0.07s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **2.50s**, across 2 poll(s), starting 0.6 minutes in.
- the bench itself went at most 2.02s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.27s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 83 | 2.27s | 2.97s | 3.30s |

## 53 segment(s) reached the bench and could not be read

- `4ddb6b2a985b`: http://127.0.0.1:10077/bytes/4ddb6b2a985b7bcca1e7359508fb36c02ffe2156b446fd9c9ade2f413d3f44f4 answered 404
- `387fa8cc43cd`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 21.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `4ebd9b17b48a`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 21.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `ed7351d81bbb`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 22.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `3fc9ecd9911f`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 22.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `f95193f2db51`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 23.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `9c26035cecc2`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 23.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `5500cae0a2a3`: the segment's first frame implies it was captured 95443.3s ago, which is before the publisher started 24.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `458260adb5cd`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 24.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `35d77214e5b6`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 24.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `ec9c252ba951`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 25.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `2a51fa6aa1cc`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 25.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `e9dd6e381c7d`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 26.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `a56256a5ef42`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 26.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `3775c6915b54`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 27.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `1d07d18b64aa`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 27.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `097499450ca4`: http://127.0.0.1:10077/bytes/097499450ca491fdd4dbf91f4deffbc5400c879d079f19ec44a6a87ff57f7dea answered 404
- `491471661d7d`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 34.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `dbf0ccc1ef3b`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 34.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `cd9905b57e55`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 35.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- ...and 33 more, in the JSON beside this file.
