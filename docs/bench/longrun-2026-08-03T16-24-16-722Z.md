# Long run: 2026-08-03T16:24:16.722Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**40 segments measured across 5.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0043**
- distance the media timeline travelled, per second of wall clock: **1.0043**
- media the timeline crossed that no segment carried: **0.00s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -256ms per minute of drift**, against the -412ms per minute measured below, which is 62% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-412ms per minute**, which across 5.1 minutes predicts -1.93s. Positive means later segments measured **more** latency.
- samples sit **0.80s** from that line, root mean square.
- **the slope is resolvable**: the 1.93s it predicts is more than twice the 0.80s the samples scatter by, so this run really did move.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **5.60s** |
| capture to fetchable, p95 | 7.33s |
| capture to fetchable, worst | 7.76s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 5.76s |
| recommended buffer (floor + 2s poll + one segment) | 9.76s |
| **behind live at that buffer** | **13.36s** |

## Does the buffer demand grow

- first third of the run needed **5.49s**, last third needed **5.33s**, a change of -0.16s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **48.28s**, across 15 poll(s), starting 0.2 minutes in.
- the bench itself went at most 6.40s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 15 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 9.76s **would have rebuffered there**, since the stall exceeded the buffer plus the segment it was playing.
- when it moved, the gateway's resolved feed index jumped **24 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 6 | 5.76s | 7.49s | 7.49s |
| 1 | 10 | 6.40s | 7.76s | 7.76s |
| 2 | 9 | 5.98s | 6.69s | 6.69s |
| 3 | 7 | 4.79s | 7.33s | 7.33s |
| 4 | 8 | 4.39s | 6.02s | 6.02s |
