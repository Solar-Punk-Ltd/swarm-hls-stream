# Long run: 2026-08-05T04:19:12.593Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 0.5s GOP

**317 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0101**
- distance the media timeline travelled, per second of wall clock: **1.0053**
- media the timeline crossed that no segment carried: **-0.86s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -319ms per minute of drift**, against the -147ms per minute measured below, which is 216% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-147ms per minute**, which across 3.0 minutes predicts -0.43s. Positive means later segments measured **more** latency.
- samples sit **0.47s** from that line, root mean square.
- **the slope is not resolvable**: the 0.43s it predicts does not clear twice the 0.47s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.40s** |
| capture to fetchable, p95 | 2.85s |
| capture to fetchable, worst | 3.74s |
| segment, median | 0.57s |
| smallest buffer that would not have stalled | 3.18s |
| recommended buffer (floor + 2s poll + one segment) | 5.74s |
| **behind live at that buffer** | **6.58s** |

## Does the buffer demand grow

- first third of the run needed **3.18s**, last third needed **2.70s**, a change of -0.48s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **2.99s**, across 2 poll(s), starting 0.6 minutes in.
- the bench itself went at most 2.16s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.74s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 108 | 1.43s | 3.27s | 3.74s |
| 1 | 107 | 1.40s | 2.22s | 3.44s |
| 2 | 102 | 1.40s | 2.11s | 3.43s |
