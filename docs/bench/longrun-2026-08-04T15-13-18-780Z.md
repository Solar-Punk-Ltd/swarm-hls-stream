# Long run: 2026-08-04T15:13:18.780Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**91 segments measured across 10.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0051**
- distance the media timeline travelled, per second of wall clock: **0.9981**
- media the timeline crossed that no segment carried: **-4.07s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for 112ms per minute of drift**, against the 7ms per minute measured below, which is 1570% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+7ms per minute**, which across 10.1 minutes predicts +0.07s. Positive means later segments measured **more** latency.
- samples sit **0.87s** from that line, root mean square.
- **the slope is not resolvable**: the 0.07s it predicts does not clear twice the 0.87s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **5.41s** |
| capture to fetchable, p95 | 7.20s |
| capture to fetchable, worst | 8.19s |
| segment, median | 1.97s |
| smallest buffer that would not have stalled | 6.19s |
| recommended buffer (floor + 2s poll + one segment) | 10.16s |
| **behind live at that buffer** | **13.60s** |

## Does the buffer demand grow

- first third of the run needed **6.19s**, last third needed **6.01s**, a change of -0.18s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **37.22s**, across 8 poll(s), starting 4.6 minutes in.
- the bench itself went at most 7.79s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 8 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 10.16s **would have rebuffered there**, since the stall exceeded the buffer plus the segment it was playing.
- when it moved, the gateway's resolved feed index jumped **19 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 8 | 5.10s | 6.41s | 6.41s |
| 1 | 8 | 5.40s | 6.62s | 6.62s |
| 2 | 14 | 6.03s | 8.19s | 8.19s |
| 3 | 9 | 5.47s | 7.21s | 7.21s |
| 4 | 7 | 5.38s | 7.02s | 7.02s |
| 5 | 9 | 5.10s | 6.10s | 6.10s |
| 6 | 10 | 5.28s | 6.50s | 6.50s |
| 7 | 10 | 4.92s | 7.98s | 7.98s |
| 8 | 9 | 5.77s | 6.50s | 6.50s |
| 9 | 7 | 5.67s | 7.20s | 7.20s |

## 1 segment(s) reached the bench and could not be read

- `c77b02a67ea4`: terminated
