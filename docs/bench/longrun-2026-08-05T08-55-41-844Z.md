# Long run: 2026-08-05T08:55:41.844Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**124 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9799**
- distance the media timeline travelled, per second of wall clock: **1.0026**
- media the timeline crossed that no segment carried: **4.06s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -157ms per minute of drift**, against the 22ms per minute measured below, which is -722% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+22ms per minute**, which across 3.1 minutes predicts +0.06s. Positive means later segments measured **more** latency.
- samples sit **0.17s** from that line, root mean square.
- **the slope is not resolvable**: the 0.06s it predicts does not clear twice the 0.17s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.94s** |
| capture to fetchable, p95 | 2.23s |
| capture to fetchable, worst | 2.70s |
| segment, median | 0.63s |
| smallest buffer that would not have stalled | 2.10s |
| recommended buffer (floor + 2s assumed client poll + one segment) | 4.73s |
| **behind live at that buffer** | **6.04s** |

## Does the buffer demand grow

- first third of the run needed **1.79s**, last third needed **2.10s**, a change of +0.30s.
- **a player configured from this run's opening third would eventually stall**, by 0.30s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **3.83s**, across 1 poll(s), starting 2.9 minutes in.
- the bench itself went at most 3.83s between two polls and 1.26s typically. **Both are measured rather than configured**, and the pair is what bounds the resolution here: the gaps are bimodal, so neither alone describes it.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **6 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 43 | 1.94s | 2.20s | 2.43s |
| 1 | 46 | 1.94s | 2.14s | 2.20s |
| 2 | 35 | 1.95s | 2.59s | 2.70s |
