# Long run: 2026-08-05T04:22:36.126Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 1s GOP

**147 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9281**
- distance the media timeline travelled, per second of wall clock: **0.9381**
- media the timeline crossed that no segment carried: **1.78s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 3717ms per minute of drift**, against the 4395ms per minute measured below, which is 85% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+4395ms per minute**, which across 3.0 minutes predicts +13.06s. Positive means later segments measured **more** latency.
- samples sit **1.85s** from that line, root mean square.
- **the slope is resolvable**: the 13.06s it predicts is more than twice the 1.85s the samples scatter by, so this run really did move.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.52s** |
| capture to fetchable, p95 | 14.31s |
| capture to fetchable, worst | 15.33s |
| segment, median | 1.13s |
| smallest buffer that would not have stalled | 14.13s |
| recommended buffer (floor + 2s poll + one segment) | 17.26s |
| **behind live at that buffer** | **20.65s** |

## Does the buffer demand grow

- first third of the run needed **4.17s**, last third needed **14.13s**, a change of +9.95s.
- **a player configured from this run's opening third would eventually stall**, by 9.95s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **3.77s**, across 2 poll(s), starting 0.1 minutes in.
- the bench itself went at most 2.65s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 17.26s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 54 | 2.94s | 4.34s | 5.44s |
| 1 | 46 | 4.53s | 8.53s | 8.72s |
| 2 | 47 | 12.17s | 14.80s | 15.33s |
