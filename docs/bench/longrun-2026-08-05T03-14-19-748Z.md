# Long run: 2026-08-05T03:14:19.748Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 0.5s GOP

**377 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.8458**
- distance the media timeline travelled, per second of wall clock: **0.8076**
- media the timeline crossed that no segment carried: **-6.80s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for 11545ms per minute of drift**, against the 16670ms per minute measured below, which is 69% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+16670ms per minute**, which across 3.0 minutes predicts +49.40s. Positive means later segments measured **more** latency.
- samples sit **4.18s** from that line, root mean square.
- **the slope is resolvable**: the 49.40s it predicts is more than twice the 4.18s the samples scatter by, so this run really did move.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **25.69s** |
| capture to fetchable, p95 | 42.97s |
| capture to fetchable, worst | 43.41s |
| segment, median | 0.40s |
| smallest buffer that would not have stalled | 43.10s |
| recommended buffer (floor + 2s poll + one segment) | 45.50s |
| **behind live at that buffer** | **70.79s** |

## Does the buffer demand grow

- first third of the run needed **8.41s**, last third needed **43.10s**, a change of +34.68s.
- **a player configured from this run's opening third would eventually stall**, by 34.68s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **1.32s**, across 1 poll(s), starting 2.2 minutes in.
- the bench itself went at most 1.32s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 134 | 2.58s | 9.84s | 11.27s |
| 1 | 94 | 23.96s | 34.46s | 35.65s |
| 2 | 149 | 41.05s | 43.23s | 43.41s |
