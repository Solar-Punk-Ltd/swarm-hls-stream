# Long run: 2026-08-05T02:47:15.554Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**73 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9686**
- distance the media timeline travelled, per second of wall clock: **1.0048**
- media the timeline crossed that no segment carried: **6.08s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -289ms per minute of drift**, against the -22ms per minute measured below, which is 1297% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-22ms per minute**, which across 3.0 minutes predicts -0.06s. Positive means later segments measured **more** latency.
- samples sit **0.68s** from that line, root mean square.
- **the slope is not resolvable**: the 0.06s it predicts does not clear twice the 0.68s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.10s** |
| capture to fetchable, p95 | 3.42s |
| capture to fetchable, worst | 4.11s |
| segment, median | 0.43s |
| smallest buffer that would not have stalled | 3.68s |
| recommended buffer (floor + 2s poll + one segment) | 6.11s |
| **behind live at that buffer** | **7.78s** |

## Does the buffer demand grow

- first third of the run needed **3.68s**, last third needed **2.79s**, a change of -0.89s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.46s**, across 2 poll(s), starting 0.0 minutes in.
- the bench itself went at most 2.09s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.11s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 32 | 1.87s | 3.90s | 4.11s |
| 1 | 20 | 2.16s | 3.11s | 3.13s |
| 2 | 21 | 2.18s | 3.11s | 3.19s |

## 325 segment(s) reached the bench and could not be read

- `ddeafcc7dbb6`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 6.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `5629d3615e99`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 6.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `2c0f7a1a5568`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 7.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `50bde16469b5`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 7.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `a9c64ce31e39`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 16.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `f6e32a2620a1`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 17.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `f2375ed00639`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 17.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `4c9b38c05d8b`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 18.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `8eb1a98a2962`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 18.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `38341d465936`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 18.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `d7ba176d3e25`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 19.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `7c50afbac4d8`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 19.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `3c6f154187b6`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 20.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `b0122cb34302`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 20.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `bb1e47521e6a`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 21.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `098a6538cf3e`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 21.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `e069fc8773ac`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 21.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `3b7a7235b521`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 22.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `6a773332d52f`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 23.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `29581756bf4a`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 23.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- ...and 305 more, in the JSON beside this file.
