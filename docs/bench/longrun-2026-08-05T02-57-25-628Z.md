# Long run: 2026-08-05T02:57:25.628Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 0.5s GOP

**294 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.8363**
- distance the media timeline travelled, per second of wall clock: **0.9965**
- media the timeline crossed that no segment carried: **28.77s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 209ms per minute of drift**, against the -36ms per minute measured below, which is -583% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-36ms per minute**, which across 3.1 minutes predicts -0.11s. Positive means later segments measured **more** latency.
- samples sit **0.65s** from that line, root mean square.
- **the slope is not resolvable**: the 0.11s it predicts does not clear twice the 0.65s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.80s** |
| capture to fetchable, p95 | 3.38s |
| capture to fetchable, worst | 4.06s |
| segment, median | 0.43s |
| smallest buffer that would not have stalled | 3.39s |
| recommended buffer (floor + 2s poll + one segment) | 5.82s |
| **behind live at that buffer** | **7.19s** |

## Does the buffer demand grow

- first third of the run needed **3.39s**, last third needed **3.09s**, a change of -0.29s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.02s**, across 2 poll(s), starting 0.6 minutes in.
- the bench itself went at most 2.13s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.82s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 99 | 1.97s | 3.76s | 4.06s |
| 1 | 96 | 1.70s | 3.27s | 3.73s |
| 2 | 99 | 1.80s | 3.44s | 3.88s |

## 53 segment(s) reached the bench and could not be read

- `5728e357e776`: cannot measure how much media 5728e357e776866816879ee9a68d5eba42ffabcd04d9d015cd7ff1f96026194a holds: it holds one video packet, which fixes when a frame started and nothing about how long it lasted. A span assumed here would be indistinguishable in the report from a measured one
- `38db5831077b`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 7.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `da6024fa3565`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 15.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `00e481b1456e`: cannot read a presentation timestamp from 00e481b1456e06d3a5480c95a6f6048b5fef6f97305c275e1ca04bbbe5cf6dcf: it holds no video packets, so the media never reached the far end even though the fetch succeeded
- `50ff13448813`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 24.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `2714876be879`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 26.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `c81a84aeb6d5`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 27.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `1db79b84e2bd`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 29.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `4aa20fe44f04`: cannot measure how much media 4aa20fe44f04ad55d78843143bfa1c20cbb176cc145fb948d1b60e02f2e9c147 holds: it holds one video packet, which fixes when a frame started and nothing about how long it lasted. A span assumed here would be indistinguishable in the report from a measured one
- `48d31ce69979`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 38.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `25c546f1bdbd`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 41.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `b9dc01c392fb`: cannot read a presentation timestamp from b9dc01c392fb0f465ad4876c73d8f12833cd6a59f33f97766a43b0879953f91f: it holds no video packets, so the media never reached the far end even though the fetch succeeded
- `847a1c97f99f`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 48.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `cecfb646fc27`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 53.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `90ea578b6de3`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 61.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `40a05694929b`: cannot measure how much media 40a05694929b9cced3d0256fd6c91a7585c9e09dd01b0fe17e8dfe84034a4299 holds: it holds one video packet, which fixes when a frame started and nothing about how long it lasted. A span assumed here would be indistinguishable in the report from a measured one
- `a75b8bd1251f`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 71.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `ca297edd49b3`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 72.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `acfe02882636`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 73.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `2c8ff935a742`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 79.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- ...and 33 more, in the JSON beside this file.
