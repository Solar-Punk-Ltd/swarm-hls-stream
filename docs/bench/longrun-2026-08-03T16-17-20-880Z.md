# Long run: 2026-08-03T16:17:20.880Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**69 segments measured across 8.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9985**
- distance the media timeline travelled, per second of wall clock: **0.9990**
- media the timeline crossed that no segment carried: **0.24s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 62ms per minute of drift**, against the 20ms per minute measured below, which is 316% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+20ms per minute**, which across 8.1 minutes predicts +0.16s. Positive means later segments measured **more** latency.
- samples sit **0.85s** from that line, root mean square.
- **the slope is not resolvable**: the 0.16s it predicts does not clear twice the 0.85s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **5.90s** |
| capture to fetchable, p95 | 6.98s |
| capture to fetchable, worst | 8.84s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 6.84s |
| recommended buffer (floor + 2s poll + one segment) | 10.84s |
| **behind live at that buffer** | **14.74s** |

## Does the buffer demand grow

- first third of the run needed **4.98s**, last third needed **5.29s**, a change of +0.31s.
- **a player configured from this run's opening third would eventually stall**, by 0.31s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **37.17s**, across 9 poll(s), starting 1.5 minutes in.
- the bench itself went at most 6.41s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 9 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 10.84s **would have rebuffered there**, since the stall exceeded the buffer plus the segment it was playing.
- when it moved, the gateway's resolved feed index jumped **18 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 10 | 5.58s | 6.98s | 6.98s |
| 1 | 8 | 5.13s | 6.67s | 6.67s |
| 2 | 8 | 6.00s | 7.83s | 7.83s |
| 3 | 8 | 6.30s | 8.84s | 8.84s |
| 4 | 9 | 6.08s | 6.46s | 6.46s |
| 5 | 7 | 6.12s | 6.76s | 6.76s |
| 6 | 8 | 5.87s | 6.71s | 6.71s |
| 7 | 10 | 5.75s | 7.29s | 7.29s |
| 8 | 1 | 5.90s | 5.90s | 5.90s |
