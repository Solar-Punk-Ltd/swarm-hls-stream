# Long run: 2026-08-05T04:05:14.553Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**357 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0075**
- distance the media timeline travelled, per second of wall clock: **1.0075**
- media the timeline crossed that no segment carried: **0.00s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -447ms per minute of drift**, against the -128ms per minute measured below, which is 349% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-128ms per minute**, which across 3.0 minutes predicts -0.38s. Positive means later segments measured **more** latency.
- samples sit **0.53s** from that line, root mean square.
- **the slope is not resolvable**: the 0.38s it predicts does not clear twice the 0.53s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.65s** |
| capture to fetchable, p95 | 2.84s |
| capture to fetchable, worst | 4.12s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 3.62s |
| recommended buffer (floor + 2s poll + one segment) | 6.12s |
| **behind live at that buffer** | **7.27s** |

## Does the buffer demand grow

- first third of the run needed **3.62s**, last third needed **2.36s**, a change of -1.26s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.51s**, across 2 poll(s), starting 0.5 minutes in.
- the bench itself went at most 2.10s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.12s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 123 | 1.70s | 3.13s | 4.12s |
| 1 | 120 | 1.52s | 2.25s | 3.64s |
| 2 | 114 | 1.68s | 2.41s | 2.86s |

## 1 segment(s) reached the bench and could not be read

- `c913365d4d71`: http://127.0.0.1:10077/bytes/c913365d4d71f5e967620b5dc145728f495090bc2832016555e225390fb7f3c5 answered 404
