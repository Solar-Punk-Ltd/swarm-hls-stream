# Long run: 2026-08-05T02:07:01.944Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**100 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.1079**
- distance the media timeline travelled, per second of wall clock: **1.0136**
- media the timeline crossed that no segment carried: **-16.83s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -818ms per minute of drift**, against the -54ms per minute measured below, which is 1506% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-54ms per minute**, which across 3.1 minutes predicts -0.16s. Positive means later segments measured **more** latency.
- samples sit **0.87s** from that line, root mean square.
- **the slope is not resolvable**: the 0.16s it predicts does not clear twice the 0.87s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **3.77s** |
| capture to fetchable, p95 | 4.90s |
| capture to fetchable, worst | 5.78s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 3.78s |
| recommended buffer (floor + 2s poll + one segment) | 7.78s |
| **behind live at that buffer** | **9.56s** |

## Does the buffer demand grow

- first third of the run needed **3.78s**, last third needed **2.98s**, a change of -0.80s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **4.02s**, across 2 poll(s), starting 0.7 minutes in.
- the bench itself went at most 2.43s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 7.78s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 34 | 3.77s | 5.57s | 5.78s |
| 1 | 33 | 3.31s | 4.82s | 4.90s |
| 2 | 33 | 3.88s | 4.81s | 5.01s |
