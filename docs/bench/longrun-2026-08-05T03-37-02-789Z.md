# Long run: 2026-08-05T03:37:02.789Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**29 segments measured across 1.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0194**
- distance the media timeline travelled, per second of wall clock: **1.0199**
- media the timeline crossed that no segment carried: **0.03s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -1192ms per minute of drift**, against the -530ms per minute measured below, which is 225% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-530ms per minute**, which across 1.1 minutes predicts -0.49s. Positive means later segments measured **more** latency.
- samples sit **0.62s** from that line, root mean square.
- **the slope is not resolvable**: the 0.49s it predicts does not clear twice the 0.62s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **3.90s** |
| capture to fetchable, p95 | 5.09s |
| capture to fetchable, worst | 5.41s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 3.41s |
| recommended buffer (floor + 2s poll + one segment) | 7.41s |
| **behind live at that buffer** | **9.32s** |

## Does the buffer demand grow

- first third of the run needed **3.41s**, last third needed **2.69s**, a change of -0.72s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.83s**, across 2 poll(s), starting 0.0 minutes in.
- the bench itself went at most 2.20s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 7.41s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 29 | 3.90s | 5.09s | 5.41s |
