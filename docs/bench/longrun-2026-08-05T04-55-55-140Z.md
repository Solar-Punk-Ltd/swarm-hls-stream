# Long run: 2026-08-05T04:55:55.140Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.25s GOP

**643 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9661**
- distance the media timeline travelled, per second of wall clock: **0.9685**
- media the timeline crossed that no segment carried: **0.43s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 1889ms per minute of drift**, against the 1721ms per minute measured below, which is 110% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+1721ms per minute**, which across 3.0 minutes predicts +5.07s. Positive means later segments measured **more** latency.
- samples sit **1.73s** from that line, root mean square.
- **the slope is resolvable**: the 5.07s it predicts is more than twice the 1.73s the samples scatter by, so this run really did move.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **8.71s** |
| capture to fetchable, p95 | 10.49s |
| capture to fetchable, worst | 10.70s |
| segment, median | 0.27s |
| smallest buffer that would not have stalled | 10.44s |
| recommended buffer (floor + 2s poll + one segment) | 12.70s |
| **behind live at that buffer** | **21.14s** |

## Does the buffer demand grow

- first third of the run needed **8.44s**, last third needed **9.92s**, a change of +1.48s.
- **a player configured from this run's opening third would eventually stall**, by 1.48s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **1.08s**, across 1 poll(s), starting -0.0 minutes in.
- the bench itself went at most 1.08s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 201 | 5.27s | 8.23s | 8.43s |
| 1 | 218 | 10.04s | 10.65s | 10.70s |
| 2 | 224 | 8.87s | 10.14s | 10.33s |
