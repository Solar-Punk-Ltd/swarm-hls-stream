# Long run: 2026-08-05T04:12:24.277Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**357 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0022**
- distance the media timeline travelled, per second of wall clock: **1.0025**
- media the timeline crossed that no segment carried: **0.07s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -153ms per minute of drift**, against the 61ms per minute measured below, which is -251% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+61ms per minute**, which across 3.1 minutes predicts +0.18s. Positive means later segments measured **more** latency.
- samples sit **0.58s** from that line, root mean square.
- **the slope is not resolvable**: the 0.18s it predicts does not clear twice the 0.58s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.54s** |
| capture to fetchable, p95 | 2.98s |
| capture to fetchable, worst | 4.28s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 3.78s |
| recommended buffer (floor + 2s poll + one segment) | 6.28s |
| **behind live at that buffer** | **7.32s** |

## Does the buffer demand grow

- first third of the run needed **2.72s**, last third needed **3.78s**, a change of +1.06s.
- **a player configured from this run's opening third would eventually stall**, by 1.06s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **3.28s**, across 2 poll(s), starting 2.5 minutes in.
- the bench itself went at most 2.11s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.28s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 120 | 1.38s | 2.46s | 3.22s |
| 1 | 122 | 1.61s | 3.18s | 3.65s |
| 2 | 115 | 1.61s | 3.31s | 4.28s |

## 1 segment(s) reached the bench and could not be read

- `b3445455879a`: http://127.0.0.1:10077/bytes/b3445455879a8a9faf61db196afbeeb1261060e4ff89383ef8f67be7a52d09ae answered 404
