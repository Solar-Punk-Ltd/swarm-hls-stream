# Long run: 2026-08-05T05:26:33.607Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**358 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0029**
- distance the media timeline travelled, per second of wall clock: **1.0029**
- media the timeline crossed that no segment carried: **0.00s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -176ms per minute of drift**, against the 16ms per minute measured below, which is -1125% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+16ms per minute**, which across 3.0 minutes predicts +0.05s. Positive means later segments measured **more** latency.
- samples sit **0.50s** from that line, root mean square.
- **the slope is not resolvable**: the 0.05s it predicts does not clear twice the 0.50s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.17s** |
| capture to fetchable, p95 | 2.69s |
| capture to fetchable, worst | 3.58s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 3.08s |
| recommended buffer (floor + 2s poll + one segment) | 5.58s |
| **behind live at that buffer** | **6.25s** |

## Does the buffer demand grow

- first third of the run needed **3.08s**, last third needed **2.71s**, a change of -0.37s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **2.97s**, across 2 poll(s), starting 0.8 minutes in.
- the bench itself went at most 2.15s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.58s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 122 | 1.17s | 2.69s | 3.58s |
| 1 | 120 | 1.16s | 2.45s | 3.17s |
| 2 | 116 | 1.16s | 2.88s | 3.21s |
