# Long run: 2026-08-05T08:45:26.945Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.25s GOP

**134 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9720**
- distance the media timeline travelled, per second of wall clock: **0.9991**
- media the timeline crossed that no segment carried: **4.84s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 54ms per minute of drift**, against the 0ms per minute measured below, which is 15308% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+0ms per minute**, which across 3.1 minutes predicts +0.00s. Positive means later segments measured **more** latency.
- samples sit **0.19s** from that line, root mean square.
- **the slope is not resolvable**: the 0.00s it predicts does not clear twice the 0.19s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.12s** |
| capture to fetchable, p95 | 2.45s |
| capture to fetchable, worst | 2.69s |
| segment, median | 0.67s |
| smallest buffer that would not have stalled | 2.02s |
| recommended buffer (floor + 2s assumed client poll + one segment) | 4.69s |
| **behind live at that buffer** | **6.14s** |

## Does the buffer demand grow

- first third of the run needed **2.02s**, last third needed **1.80s**, a change of -0.23s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **4.06s**, across 1 poll(s), starting 0.0 minutes in.
- the bench itself went at most 4.06s between two polls and 1.26s typically. **Both are measured rather than configured**, and the pair is what bounds the resolution here: the gaps are bimodal, so neither alone describes it.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **6 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 41 | 2.14s | 2.44s | 2.69s |
| 1 | 45 | 2.10s | 2.35s | 2.38s |
| 2 | 48 | 2.14s | 2.46s | 2.58s |
