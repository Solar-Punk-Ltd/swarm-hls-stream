# Long run: 2026-08-05T03:50:30.966Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**74 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0007**
- distance the media timeline travelled, per second of wall clock: **1.0027**
- media the timeline crossed that no segment carried: **0.35s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -164ms per minute of drift**, against the -100ms per minute measured below, which is 164% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-100ms per minute**, which across 3.1 minutes predicts -0.29s. Positive means later segments measured **more** latency.
- samples sit **0.42s** from that line, root mean square.
- **the slope is not resolvable**: the 0.29s it predicts does not clear twice the 0.42s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.66s** |
| capture to fetchable, p95 | 2.95s |
| capture to fetchable, worst | 3.28s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 2.78s |
| recommended buffer (floor + 2s poll + one segment) | 5.28s |
| **behind live at that buffer** | **6.44s** |

## Does the buffer demand grow

- first third of the run needed **2.68s**, last third needed **1.46s**, a change of -1.22s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **2.64s**, across 2 poll(s), starting 1.6 minutes in.
- the bench itself went at most 2.17s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.28s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 25 | 1.68s | 2.95s | 3.18s |
| 1 | 27 | 1.70s | 3.04s | 3.28s |
| 2 | 22 | 1.63s | 1.94s | 1.96s |

## 284 segment(s) reached the bench and could not be read

- `8d925f4c6a57`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 6.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `cf57aa89b722`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 6.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `09b73130ced1`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 6.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `e77c20e9b438`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 7.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `9859c52d9a02`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 7.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `343995996223`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 8.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `4c9a90be77ac`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 8.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `cf486ea3be90`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 9.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `be9083331219`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 9.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `d13b461befea`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 10.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `8aebe0500196`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 11.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `e58f8a2c4b99`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 11.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `05913d402f35`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 11.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `a610368433a4`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 12.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `289cb2baec1e`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 12.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `5e9ab85f9a5f`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 18.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `c80ab0c49026`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 18.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `205d294e8bf1`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 18.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `5eefaa630a93`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 19.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `88447cda8ab1`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 19.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- ...and 264 more, in the JSON beside this file.
