# Long run: 2026-08-05T03:07:34.300Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 2s GOP

**267 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.5989**
- distance the media timeline travelled, per second of wall clock: **0.6064**
- media the timeline crossed that no segment carried: **1.33s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 23614ms per minute of drift**, against the 23913ms per minute measured below, which is 99% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+23913ms per minute**, which across 3.0 minutes predicts +70.80s. Positive means later segments measured **more** latency.
- samples sit **1.07s** from that line, root mean square.
- **the slope is resolvable**: the 70.80s it predicts is more than twice the 1.07s the samples scatter by, so this run really did move.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **31.64s** |
| capture to fetchable, p95 | 68.48s |
| capture to fetchable, worst | 72.05s |
| segment, median | 0.40s |
| smallest buffer that would not have stalled | 71.78s |
| recommended buffer (floor + 2s poll + one segment) | 74.18s |
| **behind live at that buffer** | **105.42s** |

## Does the buffer demand grow

- first third of the run needed **22.91s**, last third needed **71.78s**, a change of +48.88s.
- **a player configured from this run's opening third would eventually stall**, by 48.88s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **1.89s**, across 1 poll(s), starting 0.2 minutes in.
- the bench itself went at most 1.89s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 99 | 13.28s | 22.99s | 23.30s |
| 1 | 86 | 33.90s | 45.89s | 47.11s |
| 2 | 82 | 59.40s | 71.63s | 72.05s |
