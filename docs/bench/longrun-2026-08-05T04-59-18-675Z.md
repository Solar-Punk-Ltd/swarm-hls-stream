# Long run: 2026-08-05T04:59:18.675Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**355 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9953**
- distance the media timeline travelled, per second of wall clock: **0.9953**
- media the timeline crossed that no segment carried: **0.00s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for 283ms per minute of drift**, against the 134ms per minute measured below, which is 211% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+134ms per minute**, which across 3.0 minutes predicts +0.40s. Positive means later segments measured **more** latency.
- samples sit **0.60s** from that line, root mean square.
- **the slope is not resolvable**: the 0.40s it predicts does not clear twice the 0.60s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.63s** |
| capture to fetchable, p95 | 3.12s |
| capture to fetchable, worst | 4.20s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 3.70s |
| recommended buffer (floor + 2s poll + one segment) | 6.20s |
| **behind live at that buffer** | **7.33s** |

## Does the buffer demand grow

- first third of the run needed **3.05s**, last third needed **3.70s**, a change of +0.65s.
- **a player configured from this run's opening third would eventually stall**, by 0.65s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **2.96s**, across 2 poll(s), starting 2.9 minutes in.
- the bench itself went at most 2.11s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.20s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 121 | 1.50s | 2.92s | 3.55s |
| 1 | 121 | 1.49s | 2.70s | 3.64s |
| 2 | 113 | 1.85s | 3.50s | 4.20s |
