# Playing a recording back, and seeking inside it

**2026-09-04T07:48:38.891Z.** Chrome 151.0.7922.75, `http://127.0.0.1:10074/#/watch/video/8d8a30ff4cbcf8ad0e0773547686295f8157feb0/3a989702-526b-4eef-9ac6-4aa0a6b802c9?qoe=1`.

Segment bytes came from **weeb3**, which is what the client reports rather than what was asked for. The arm settled for 60.0s, and its proof window opened where that settle ended. Only requests from that instant on decide whether this run is the condition it is filed under.

Instrument: **SOUND**

## What the player was handed

| | |
| --- | ---: |
| duration at the first frame | 120.90s |
| duration after settling | 120.90s |
| seekable to | 120.90s |
| buffered ahead | 120.90s |
| position after settling | 68.06s |

A finite duration is the whole point: a live playlist reports `Infinity` here, so this
is what says the player received a finished playlist rather than a live window. The two
duration rows are separate because they have been seen to disagree, and a seek target
comes off `seekable`, which is what a seek can actually reach.

## The ladder this recording offered

The player parsed **4 rungs**: 360p, 480p, 720p, 1080p.

It rode 1080p across the run.

⛔ A recording whose master resolved and whose upper rung playlists did not plays
perfectly at its bottom rung. Every other reading in this report would call that a pass,
which is why the rung list is here and is read from the shipped overlay rather than from
the media element.

### What each rung holds

| rung | segments | duration | ends at | read from |
| --- | ---: | ---: | --- | --- |
| 360p | 61 | 121.73s | `702c41bef0c1…` | player |
| 480p | 61 | 121.90s | `97c169174611…` | player |
| 720p | 61 | 121.43s | `36014850d62d…` | feed |
| 1080p | 61 | 120.90s | `599d9b7195e4…` | player |

hls.js loads a level playlist only when it plays that level, so a rung read from its own
feed is the ordinary case rather than a fault. What a suite judges on is the last
reference against the last segment the uploader published on that rung, with no tolerance.

## Seeking

| target | asked | landed | landed in | resumed in | |
| --- | ---: | ---: | ---: | ---: | --- |
| 50% | 60.45s | 60.46s | 152ms | 326ms | ✅ |
| 90% | 108.81s | 108.82s | 153ms | 342ms | ✅ |
| 20% | 24.18s | 24.19s | 84ms | 344ms | ✅ |

The last target is backwards on purpose: a forward seek can be served by what the player
already buffered, and a backward one into a discarded region cannot.

### What the player did with what it was handed

| | |
| --- | --- |
| probe installed | true |
| networkState | 2 |
| source buffers | audio/mp4;codecs=mp4a.40.2, video/mp4;codecs=avc1.4d4028 |
| appends | 130, 69,950,505 bytes |


#### What each track holds

`HTMLMediaElement.buffered` is the intersection of these, so the shortest one sets the
whole timeline and the element cannot say which track did it.

| track | buffered |
| --- | --- |
| `audio/mp4;codecs=mp4a.40.2` | 0.000 - 120.771 |
| `video/mp4;codecs=avc1.4d4028` | 0.000 - 120.901 |

#### The media elements on the page

| # | readyState | currentTime | paused | muted | autoplay | buffered | error |
| --- | ---: | ---: | --- | --- | --- | --- | --- |
| 0 | 4 | 26.55s | false | true | true | 0.00-120.90 | none |

#### What the element did, in order

- `4351ms` play() called (readyState 0)
- `4353ms` play (readyState 0)
- `4355ms` waiting (readyState 0)
- `5288ms` play() called (readyState 0)
- `6707ms` loadedmetadata (readyState 1)
- `6818ms` loadeddata (readyState 4)
- `6818ms` canplay (readyState 4)
- `6818ms` playing (readyState 4)
- `6820ms` play() resolved (readyState 4)
- `6820ms` play() resolved (readyState 4)
- `6820ms` canplaythrough (readyState 4)
- `74919ms` seeking (readyState 1)
- `74924ms` waiting (readyState 1)
- `75043ms` seeked (readyState 4)
- `75044ms` canplay (readyState 4)
- `75044ms` playing (readyState 4)
- `75044ms` canplaythrough (readyState 4)
- `75402ms` seeking (readyState 1)
- `75403ms` waiting (readyState 1)
- `75547ms` seeked (readyState 4)
- `75548ms` canplay (readyState 4)
- `75548ms` playing (readyState 4)
- `75548ms` canplaythrough (readyState 4)
- `75901ms` seeking (readyState 1)
- `75901ms` waiting (readyState 1)
- `75977ms` seeked (readyState 4)
- `75978ms` canplay (readyState 4)
- `75978ms` playing (readyState 4)
- `75978ms` canplaythrough (readyState 4)

## What this run consumed

Read off the deployment either side of the run rather than estimated, over 1.4 minutes.

| node | postage, fullest bucket | per min | chequebook BZZ | per min | postage runway |
| --- | ---: | ---: | ---: | ---: | ---: |
| 360p :10075 | 40/256 → 40/256 | 0.00 | 6.191 → 6.191 | 0.0000 | not measurable from this run |
| 480p :11071 | 49/256 → 49/256 | 0.00 | 2.264 → 2.264 | 0.0000 | not measurable from this run |
| 720p :11073 | 85/256 → 85/256 | 0.00 | 0.661 → 0.661 | 0.0000 | not measurable from this run |
| 1080p :11075 | 124/256 → 124/256 | 0.00 | 1.604 → 1.604 | 0.0000 | not measurable from this run |
| **whole stage** | 0 buckets used | 0.00 | 0.000 spent | 0.0000 | not measurable from this run |

At this run's own rate: **not measurable from this run of postage**, **not measurable from this run of BZZ**, and the first batch expires in 4.1 days.

⚠️ Both runways are the **shortest** any one node reported, never the stage's total. The stage stops when the first rung fills or runs dry, and across the ladder 1080p burns roughly seven times the bytes of 360p.

**0.00000 BZZ per megabyte delivered.** Carry this one to another bitrate, never the per-minute rate: seventeen runs at a fixed 0.25s segment measured 0.0179 BZZ/min at 720p against 0.0389 at 1080p, and sat inside 0.00081 to 0.00096 per megabyte across that same 2.5x spread. It does **not** carry across segment lengths, where a controlled comparison put the gap at 23.5%.

The postage runway is a **floor**. `utilization` is the fullest of sixty-five thousand buckets, and a maximum grows fastest while the batch is nearly empty and then flattens, so an early run overstates the long-run rate. **Two runs at different fullness are not comparable at all**, which is what retracted the postage half of the 2026-08-07 segment-length comparison. Later runs on the same batch, at similar fullness, are the ones to believe.

⚠️ **Before planning the next run:**

- the 720p chequebook is down to 0.661 BZZ. It is restored only by a deposit, never by peers cashing.
