# Long run: 2026-08-05T05:29:58.588Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 1s GOP

**178 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0056**
- distance the media timeline travelled, per second of wall clock: **1.0066**
- media the timeline crossed that no segment carried: **0.18s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -394ms per minute of drift**, against the -47ms per minute measured below, which is 833% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-47ms per minute**, which across 3.1 minutes predicts -0.14s. Positive means later segments measured **more** latency.
- samples sit **0.74s** from that line, root mean square.
- **the slope is not resolvable**: the 0.14s it predicts does not clear twice the 0.74s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.36s** |
| capture to fetchable, p95 | 3.79s |
| capture to fetchable, worst | 4.18s |
| segment, median | 1.00s |
| smallest buffer that would not have stalled | 3.18s |
| recommended buffer (floor + 2s poll + one segment) | 6.18s |
| **behind live at that buffer** | **7.54s** |

## Does the buffer demand grow

- first third of the run needed **3.18s**, last third needed **2.95s**, a change of -0.23s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.45s**, across 2 poll(s), starting 0.2 minutes in.
- the bench itself went at most 2.59s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 6.18s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 60 | 2.36s | 3.78s | 4.18s |
| 1 | 62 | 2.45s | 3.75s | 3.98s |
| 2 | 56 | 2.16s | 3.93s | 3.95s |
