# Long run: 2026-08-05T08:52:18.016Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.5s GOP

**67 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0092**
- distance the media timeline travelled, per second of wall clock: **1.0092**
- media the timeline crossed that no segment carried: **0.00s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -554ms per minute of drift**, against the -42ms per minute measured below, which is 1309% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-42ms per minute**, which across 3.1 minutes predicts -0.13s. Positive means later segments measured **more** latency.
- samples sit **0.24s** from that line, root mean square.
- **the slope is not resolvable**: the 0.13s it predicts does not clear twice the 0.24s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.61s** |
| capture to fetchable, p95 | 1.73s |
| capture to fetchable, worst | 3.42s |
| segment, median | 0.50s |
| smallest buffer that would not have stalled | 2.92s |
| recommended buffer (floor + 2s assumed client poll + one segment) | 5.42s |
| **behind live at that buffer** | **6.53s** |

## Does the buffer demand grow

- first third of the run needed **2.92s**, last third needed **1.27s**, a change of -1.65s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **10.43s**, across 1 poll(s), starting 1.9 minutes in.
- the bench itself went at most 10.43s between two polls and 2.25s typically. **Both are measured rather than configured**, and the pair is what bounds the resolution here: the gaps are bimodal, so neither alone describes it.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **21 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 22 | 1.59s | 1.71s | 3.42s |
| 1 | 24 | 1.62s | 1.69s | 1.73s |
| 2 | 21 | 1.62s | 1.75s | 1.77s |
