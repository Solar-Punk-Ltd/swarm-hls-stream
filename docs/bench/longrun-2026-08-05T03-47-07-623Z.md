# Long run: 2026-08-05T03:47:07.623Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**90 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0076**
- distance the media timeline travelled, per second of wall clock: **1.0076**
- media the timeline crossed that no segment carried: **0.00s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for -459ms per minute of drift**, against the -82ms per minute measured below, which is 557% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-82ms per minute**, which across 3.1 minutes predicts -0.24s. Positive means later segments measured **more** latency.
- samples sit **0.65s** from that line, root mean square.
- **the slope is not resolvable**: the 0.24s it predicts does not clear twice the 0.65s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **3.97s** |
| capture to fetchable, p95 | 5.09s |
| capture to fetchable, worst | 5.29s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 3.29s |
| recommended buffer (floor + 2s poll + one segment) | 7.29s |
| **behind live at that buffer** | **9.26s** |

## Does the buffer demand grow

- first third of the run needed **3.20s**, last third needed **3.20s**, a change of +0.01s.
- **a player configured from this run's opening third would eventually stall**, by 0.01s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **4.21s**, across 2 poll(s), starting 1.9 minutes in.
- the bench itself went at most 2.22s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 7.29s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 31 | 3.97s | 5.13s | 5.20s |
| 1 | 29 | 4.01s | 5.06s | 5.29s |
| 2 | 30 | 3.93s | 5.09s | 5.20s |
