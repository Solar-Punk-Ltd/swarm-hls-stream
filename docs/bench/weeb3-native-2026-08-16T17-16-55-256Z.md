# weeb-3's own page, our broadcast, no gateway

**2026-08-16T17:16:55.256Z.** `https://lat-murmeldjur.github.io/weeb-3/#/live/stream/8d8a30ff4cbcf8ad0e0773547686295f8157feb0/be608ecf-6a84-4301-bfb3-3f4e0f268f67`

| | |
| --- | ---: |
| gateway-less | ✅ **yes**, no off-shell host contacted |
| realtimeRatio, whole window | 1 over 239.1s |
| **realtimeRatio, once moving** | **1** |
| startup before the playhead moved | 1s |
| stalls | 2 |
| mean buffer ahead | 5858.54s |
| segments in the log panel, done / failed | 24 / 0 |
| ⛔ that is a ROLLING WINDOW, not a total | 12s of media against a 239.1s window |
| mean segment | 0.172 MB, 0.5s |
| resolutions seen | 1280x720 |
| peers at end | 200 |
| requests, all hosts | 678 |
| off-shell contact | {"docs.libp2p.io":2} (served bytes: {}) |
| arm | recording, sought to have media ahead |
| edge advanced across the window | 0.0s |
| distance from the **appended** edge, median | 5859.91s (max 5979.47s) |
| **behind production, start → end** | ⚠️ no publisher clock, this arm cannot be ranked |
| media ran out inside the window | no |

## What the bee nodes themselves recorded over the same window

```
not collected here: recording-timeline-arms brackets this arm
```

⛔⛔ **The appended-edge distance is NOT hls.js's `latency` and cannot be compared with one.**
weeb-3's page exposes no player handle, so that figure is unreachable from outside it. An edge
only moves once a segment has been fetched and appended, so a SLOWER node reports a SMALLER
distance. Rank arms on the behind-production row, which is read off the publisher's clock.

⚠️ The visibility sensor passes by construction here, because Playwright forces a visible page.
Instrument verdict: sound
