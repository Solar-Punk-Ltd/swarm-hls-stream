# Long run: 2026-08-05T04:29:31.269Z

engine `srs`, profile `latbench`, publishing 1920x1080 @ 30fps, 6000kbps, 2s GOP

**81 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9889**
- distance the media timeline travelled, per second of wall clock: **1.0001**
- media the timeline crossed that no segment carried: **2.03s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -8ms per minute of drift**, against the 156ms per minute measured below, which is -5% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+156ms per minute**, which across 3.1 minutes predicts +0.47s. Positive means later segments measured **more** latency.
- samples sit **0.71s** from that line, root mean square.
- **the slope is not resolvable**: the 0.47s it predicts does not clear twice the 0.71s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.78s** |
| capture to fetchable, p95 | 5.90s |
| capture to fetchable, worst | 6.12s |
| segment, median | 2.23s |
| smallest buffer that would not have stalled | 3.87s |
| recommended buffer (floor + 2s poll + one segment) | 8.10s |
| **behind live at that buffer** | **10.65s** |

## Does the buffer demand grow

- first third of the run needed **3.86s**, last third needed **3.87s**, a change of +0.01s.
- **a player configured from this run's opening third would eventually stall**, by 0.01s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **4.36s**, across 2 poll(s), starting 2.3 minutes in.
- the bench itself went at most 2.48s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 8.10s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 27 | 4.78s | 5.90s | 6.12s |
| 1 | 27 | 4.72s | 5.87s | 5.91s |
| 2 | 26 | 4.56s | 5.81s | 5.99s |
| 3 | 1 | 6.10s | 6.10s | 6.10s |
