# Long run: 2026-08-04T14:30:27.341Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**69 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9998**
- distance the media timeline travelled, per second of wall clock: **1.0024**
- media the timeline crossed that no segment carried: **0.47s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -145ms per minute of drift**, against the 155ms per minute measured below, which is -93% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+155ms per minute**, which across 3.1 minutes predicts +0.46s. Positive means later segments measured **more** latency.
- samples sit **0.60s** from that line, root mean square.
- **the slope is not resolvable**: the 0.46s it predicts does not clear twice the 0.60s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.80s** |
| capture to fetchable, p95 | 5.96s |
| capture to fetchable, worst | 6.11s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 4.11s |
| recommended buffer (floor + 2s poll + one segment) | 8.11s |
| **behind live at that buffer** | **10.91s** |

## Does the buffer demand grow

- first third of the run needed **3.74s**, last third needed **3.92s**, a change of +0.18s.
- **a player configured from this run's opening third would eventually stall**, by 0.18s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **4.66s**, across 2 poll(s), starting 2.5 minutes in.
- the bench itself went at most 2.59s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 8.11s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 23 | 4.61s | 5.65s | 5.71s |
| 1 | 20 | 4.87s | 6.02s | 6.11s |
| 2 | 26 | 4.73s | 5.96s | 5.99s |

## 21 segment(s) reached the bench and could not be read

- `506a4d1e72a5`: terminated
- `eb8103e34699`: terminated
- `03848ec9bf42`: http://127.0.0.1:10077/bytes/03848ec9bf422d2720bca03961de688e6f0b4210cb9fec0065db8b7fdb4c353f answered 404
- `7730ec9ee638`: http://127.0.0.1:10077/bytes/7730ec9ee63865f0e0a96d76a9c7c85060fbdb1e743a506fbd4dd2c4499c3a92 answered 404
- `ac25f874256b`: terminated
- `10af5751d9ab`: terminated
- `ab1092abf96b`: terminated
- `60cbc7dbd464`: terminated
- `9709da6e4c2d`: terminated
- `7191a36add03`: terminated
- `1f31513a508e`: terminated
- `4e51bfea7d72`: terminated
- `b5d072574016`: terminated
- `8a07ad0418ad`: terminated
- `ff04b3c89915`: terminated
- `4acf46261704`: terminated
- `744211b7ded1`: http://127.0.0.1:10077/bytes/744211b7ded15fa90181a3d25b571cf5bf3b61db874fb25f5ce38169a40aa792 answered 404
- `44d22b3d0092`: http://127.0.0.1:10077/bytes/44d22b3d00929385f43ca3d38c5970f8a36658913eed868d2754aaf5b4708f71 answered 404
- `72c9e325024f`: http://127.0.0.1:10077/bytes/72c9e325024f4c22dfd2cc5ff92fe9cf14627caef336466dc0f3df71c7f2ad4f answered 404
- `8b311a042ebb`: terminated
- ...and 1 more, in the JSON beside this file.
