# Long run: 2026-08-05T03:24:28.152Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**140 segments measured across 3.0 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0165**
- distance the media timeline travelled, per second of wall clock: **0.9998**
- media the timeline crossed that no segment carried: **-2.87s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for 9ms per minute of drift**, against the -80ms per minute measured below, which is -12% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-80ms per minute**, which across 3.0 minutes predicts -0.23s. Positive means later segments measured **more** latency.
- samples sit **0.44s** from that line, root mean square.
- **the slope is not resolvable**: the 0.23s it predicts does not clear twice the 0.44s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.92s** |
| capture to fetchable, p95 | 2.81s |
| capture to fetchable, worst | 3.12s |
| segment, median | 0.43s |
| smallest buffer that would not have stalled | 2.66s |
| recommended buffer (floor + 2s poll + one segment) | 5.09s |
| **behind live at that buffer** | **6.57s** |

## Does the buffer demand grow

- first third of the run needed **2.66s**, last third needed **2.52s**, a change of -0.13s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **2.52s**, across 2 poll(s), starting 0.3 minutes in.
- the bench itself went at most 2.17s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 5.09s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 68 | 1.94s | 2.85s | 3.12s |
| 1 | 55 | 1.99s | 2.92s | 2.93s |
| 2 | 17 | 1.69s | 2.09s | 2.09s |

## 281 segment(s) reached the bench and could not be read

- `07c7946bc26d`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 13.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `d99361187592`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 13.5s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `f21bd0413d9c`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 13.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `407c45f4e636`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 14.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `0d1f5418e681`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 17.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `ce9d8569f3cf`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 18.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `03c5a82705ff`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 18.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `b6937577138d`: the segment's first frame implies it was captured 95443.6s ago, which is before the publisher started 19.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `aea6249a8238`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 19.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `be97cd536aa5`: the segment's first frame implies it was captured 95443.3s ago, which is before the publisher started 19.7s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `3944e9b693b6`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 25.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `435e05126ce2`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 26.1s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `dfe4618b1d14`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 26.6s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `72b84df7f8fc`: the segment's first frame implies it was captured 95443.3s ago, which is before the publisher started 26.9s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `1aa724802d4e`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 27.4s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `6190dfe720da`: the segment's first frame implies it was captured 95443.5s ago, which is before the publisher started 28.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `efe53967e9e2`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 28.3s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `d35fa4f17a5d`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 28.8s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `6bdf4fa1d80e`: the segment's first frame implies it was captured 95443.4s ago, which is before the publisher started 29.2s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- `e174c3af1428`: the segment's first frame implies it was captured 95443.7s ago, which is before the publisher started 32.0s ago, which cannot happen. The wall-clock timestamps the publisher wrote did not survive the pipeline, most likely because the media engine rebased the stream to start at zero when it repackaged. Latency cannot be measured this way against this deployment; the per-hop split from the uploader log still can.
- ...and 261 more, in the JSON beside this file.
