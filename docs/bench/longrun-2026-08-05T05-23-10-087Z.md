# Long run: 2026-08-05T05:23:10.087Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.25s GOP

**663 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9936**
- distance the media timeline travelled, per second of wall clock: **0.9961**
- media the timeline crossed that no segment carried: **0.44s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 235ms per minute of drift**, against the -215ms per minute measured below, which is -109% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-215ms per minute**, which across 3.1 minutes predicts -0.64s. Positive means later segments measured **more** latency.
- samples sit **0.40s** from that line, root mean square.
- **the slope is not resolvable**: the 0.64s it predicts does not clear twice the 0.40s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **2.58s** |
| capture to fetchable, p95 | 3.62s |
| capture to fetchable, worst | 3.74s |
| segment, median | 0.27s |
| smallest buffer that would not have stalled | 3.47s |
| recommended buffer (floor + 2s poll + one segment) | 5.74s |
| **behind live at that buffer** | **8.05s** |

## Does the buffer demand grow

- first third of the run needed **3.47s**, last third needed **2.37s**, a change of -1.10s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **0.63s**, across 1 poll(s), starting 0.0 minutes in.
- the bench itself went at most 0.63s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 219 | 2.71s | 3.67s | 3.74s |
| 1 | 228 | 2.70s | 3.30s | 3.39s |
| 2 | 216 | 2.40s | 2.59s | 2.64s |
