# Long run: 2026-08-03T16:35:50.917Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**42 segments measured across 6.2 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9934**
- distance the media timeline travelled, per second of wall clock: **0.9939**
- media the timeline crossed that no segment carried: **0.17s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for 366ms per minute of drift**, against the 215ms per minute measured below, which is 170% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+215ms per minute**, which across 6.2 minutes predicts +1.23s. Positive means later segments measured **more** latency.
- samples sit **0.74s** from that line, root mean square.
- **the slope is not resolvable**: the 1.23s it predicts does not clear twice the 0.74s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.81s** |
| capture to fetchable, p95 | 6.18s |
| capture to fetchable, worst | 6.82s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 4.82s |
| recommended buffer (floor + 2s poll + one segment) | 8.82s |
| **behind live at that buffer** | **11.63s** |

## Does the buffer demand grow

- first third of the run needed **3.43s**, last third needed **4.82s**, a change of +1.40s.
- **a player configured from this run's opening third would eventually stall**, by 1.40s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **53.51s**, across 17 poll(s), starting 0.2 minutes in.
- the bench itself went at most 6.33s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 17 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 8.82s **would have rebuffered there**, since the stall exceeded the buffer plus the segment it was playing.
- when it moved, the gateway's resolved feed index jumped **26 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 5 | 4.06s | 4.66s | 4.66s |
| 1 | 4 | 4.49s | 5.43s | 5.43s |
| 2 | 9 | 4.89s | 5.51s | 5.51s |
| 3 | 6 | 4.11s | 6.01s | 6.01s |
| 4 | 10 | 4.76s | 6.82s | 6.82s |
| 5 | 8 | 5.65s | 6.42s | 6.42s |
