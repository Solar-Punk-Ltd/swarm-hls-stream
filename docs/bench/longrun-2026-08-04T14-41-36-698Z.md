# Long run: 2026-08-04T14:41:36.698Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**280 segments measured across 10.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **0.9999**
- distance the media timeline travelled, per second of wall clock: **0.9994**
- media the timeline crossed that no segment carried: **-0.30s** across the run, so the segments tile the timeline and a viewer misses nothing.

**The publisher's own pacing accounts for 38ms per minute of drift**, against the 18ms per minute measured below, which is 215% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+18ms per minute**, which across 10.1 minutes predicts +0.18s. Positive means later segments measured **more** latency.
- samples sit **0.71s** from that line, root mean square.
- **the slope is not resolvable**: the 0.18s it predicts does not clear twice the 0.71s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.84s** |
| capture to fetchable, p95 | 6.11s |
| capture to fetchable, worst | 8.31s |
| segment, median | 2.00s |
| smallest buffer that would not have stalled | 6.31s |
| recommended buffer (floor + 2s poll + one segment) | 10.31s |
| **behind live at that buffer** | **13.14s** |

## Does the buffer demand grow

- first third of the run needed **4.23s**, last third needed **4.37s**, a change of +0.14s.
- **a player configured from this run's opening third would eventually stall**, by 0.14s. That is what every buffer recommendation in `profiles.md` was derived from, since each of those runs is roughly one third of this one long.

## The longest a viewer waited

- the feed named the same newest segment for **4.59s**, across 2 poll(s), starting 7.9 minutes in.
- the bench itself went at most 2.55s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 10.31s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 29 | 4.51s | 5.89s | 6.00s |
| 1 | 29 | 4.85s | 6.11s | 6.23s |
| 2 | 27 | 4.79s | 5.99s | 6.16s |
| 3 | 30 | 5.26s | 6.74s | 8.31s |
| 4 | 26 | 5.13s | 6.29s | 6.38s |
| 5 | 29 | 5.22s | 6.89s | 7.02s |
| 6 | 28 | 4.78s | 5.99s | 6.37s |
| 7 | 27 | 4.78s | 5.98s | 6.33s |
| 8 | 28 | 4.77s | 6.00s | 6.11s |
| 9 | 27 | 4.92s | 6.03s | 6.10s |

## 19 segment(s) reached the bench and could not be read

- `8d5b0692a6e7`: terminated
- `7c2f213cf909`: terminated
- `88f13369145d`: terminated
- `4b4c0b9fdca2`: terminated
- `7d3eca66d28d`: terminated
- `2facba7c22f9`: terminated
- `df810fc4e449`: http://127.0.0.1:10077/bytes/df810fc4e4491b866829fd8454a50a69ce9fb6394cb49cf386036f0070794b93 answered 404
- `0d8f5325449a`: terminated
- `199f492d7ed2`: terminated
- `1c063f53653c`: terminated
- `bacce5a909ad`: terminated
- `06318896c226`: terminated
- `c637d0eb65a9`: terminated
- `c33073fd8257`: terminated
- `fffa0a47a9e7`: terminated
- `0ed89c9030e0`: http://127.0.0.1:10077/bytes/0ed89c9030e0b4409da8b78a5b76b207e1897ee25d7c3acd528029bbfe8a36a2 answered 404
- `1512b6c1bc67`: terminated
- `60cb7bc4e3bb`: terminated
- `81c6ce085191`: terminated
