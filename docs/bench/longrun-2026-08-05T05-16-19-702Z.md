# Long run: 2026-08-05T05:16:19.702Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 2s GOP

**88 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0008**
- distance the media timeline travelled, per second of wall clock: **1.0071**
- media the timeline crossed that no segment carried: **1.13s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -428ms per minute of drift**, against the -17ms per minute measured below, which is 2521% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **-17ms per minute**, which across 3.1 minutes predicts -0.05s. Positive means later segments measured **more** latency.
- samples sit **0.68s** from that line, root mean square.
- **the slope is not resolvable**: the 0.05s it predicts does not clear twice the 0.68s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **4.05s** |
| capture to fetchable, p95 | 5.04s |
| capture to fetchable, worst | 5.14s |
| segment, median | 2.03s |
| smallest buffer that would not have stalled | 3.04s |
| recommended buffer (floor + 2s poll + one segment) | 7.08s |
| **behind live at that buffer** | **9.09s** |

## Does the buffer demand grow

- first third of the run needed **3.04s**, last third needed **3.01s**, a change of -0.03s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **3.82s**, across 2 poll(s), starting 0.4 minutes in.
- the bench itself went at most 2.44s between two polls, against a 2.00s cadence. Nothing shorter than that is observable here.
- **that stall is the feed's**: the bench asked 2 times inside it and got the same answer every time, so a viewer polling at this cadence saw the stream stop for that long.
- a player holding 7.08s would have played through it.
- when it moved, the gateway's resolved feed index jumped **1 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 31 | 4.11s | 5.06s | 5.10s |
| 1 | 28 | 3.73s | 4.74s | 5.14s |
| 2 | 29 | 3.99s | 5.04s | 5.05s |
