# Long run: 2026-08-05T08:59:07.196Z

engine `srs`, profile `latbench`, publishing 1280x720 @ 30fps, 2500kbps, 0.25s GOP

**33 segments measured across 3.1 minutes of one continuous broadcast.** Every other report in this directory covers the opening seconds of a fresh one.

## Is the instrument honest

- media the segments carried, per second of wall clock: **1.0013**
- distance the media timeline travelled, per second of wall clock: **1.0038**
- media the timeline crossed that no segment carried: **0.45s** across the run. A viewer sees that much of the broadcast simply missing, in jumps, and no latency column shows it.

**The publisher's own pacing accounts for -229ms per minute of drift**, against the 5ms per minute measured below, which is -5054% of it. The publisher is paced by ffmpeg's `realtime` filter at the nominal frame rate, and a real broadcaster sending from a camera has no equivalent, so subtract this before reading the drift as the deployment.

## Did the latency hold still

- fitted slope: **+5ms per minute**, which across 3.1 minutes predicts +0.01s. Positive means later segments measured **more** latency.
- samples sit **0.20s** from that line, root mean square.
- **the slope is not resolvable**: the 0.01s it predicts does not clear twice the 0.20s the samples scatter by, so this run is flat within its own noise and the slope is a fit through scatter.

## What a viewer experienced

| | |
| --- | --- |
| capture to fetchable, median | **1.00s** |
| capture to fetchable, p95 | 1.41s |
| capture to fetchable, worst | 1.69s |
| segment, median | 0.27s |
| smallest buffer that would not have stalled | 1.42s |
| recommended buffer (floor + 2s assumed client poll + one segment) | 3.69s |
| **behind live at that buffer** | **4.43s** |

## Does the buffer demand grow

- first third of the run needed **1.42s**, last third needed **1.14s**, a change of -0.29s.
- the demand did not grow, so a buffer derived from this run’s opening third would still have held at the end of it.

## The longest a viewer waited

- the feed named the same newest segment for **8.73s**, across 1 poll(s), starting 0.6 minutes in.
- the bench itself went at most 8.73s between two polls and 7.27s typically. **Both are measured rather than configured**, and the pair is what bounds the resolution here: the gaps are bimodal, so neither alone describes it.
- **that gap is this instrument's, not the feed's.** Only one poll fell inside it, so the feed may have advanced any number of times unobserved. Read it as the resolution of the measurement rather than as a stall.
- whether a player would have rebuffered cannot be said from this run, for the reason above.
- when it moved, the gateway's resolved feed index jumped **32 updates**. A reader keeping up moves one at a time, so that many were already written and waiting: the writer did not pause, the reader was behind it.

## Minute by minute

| minute | samples | median | p95 | worst |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 9 | 1.03s | 1.69s | 1.69s |
| 1 | 10 | 0.90s | 1.07s | 1.07s |
| 2 | 14 | 1.00s | 1.41s | 1.41s |

## 1 segment(s) reached the bench and could not be read

- `754eea4b510d`: http://127.0.0.1:10077/bytes/754eea4b510d3250c9a563963ba42547e81ace56dcae3c0a9c312fa99e8b9561 answered 404
